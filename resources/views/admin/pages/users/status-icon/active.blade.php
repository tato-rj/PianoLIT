<div class="text-success" title="{{$user->first_name}}'subscription is active and set to renew on {{$user->subscription->renews_at->toFormattedDateString()}}">
	<i class="fas fa-check-circle"></i>
</div>
